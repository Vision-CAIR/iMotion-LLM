# data

This directory documents the expected dataset layout only.

Raw datasets, caches, and generated artifacts should remain outside version control unless a small metadata file is explicitly needed.
